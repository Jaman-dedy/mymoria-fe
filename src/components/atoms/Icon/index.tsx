// /components/atoms/Icon.tsx
import React from 'react';

interface IconProps {
  // Your icon properties
}

const Icon: React.FC<IconProps> = ({ /* Your icon properties here */ }) => {
  return (
    // Your icon component implementation
    // This can be an SVG, Font Awesome, or any other icon implementation
    <span>Icon</span>
  );
};

export default Icon;
